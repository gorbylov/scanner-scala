package com.scanner.service.currency

/**
  * Created by Iurii on 06-03-2017.
  */
class CurrencyServiceTest {
//  val future = service ? ConvertCurrencyQuery("EUR", "UAH", 100)
//  future.map(any => any.asInstanceOf[ConvertCurrencyResponse])
//    .onComplete(
//      tryResponse => tryResponse.fold(
//        err => log.error("ERROR!!!"),
//        res => log.info(s"WAZZZAP: ${res.value}")
//      )
//    )
}
